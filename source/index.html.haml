---
pageable: true
---

- page_articles.each_with_index do |article, i|
  %article
    %h2
      = link_to article.title, article
      %span= date_tag(article.date, :short)
    ~ article.summary
    - if article.summary != article.body
      = link_to '...', article, class: 'readmore'
- if paginate
  .pagination
    - if prev_page
      = link_to '< Newer posts', prev_page, class: 'prev_page'
    - if next_page
      = link_to 'Older posts >', next_page, class: 'next_page'
