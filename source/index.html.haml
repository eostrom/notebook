---
pageable: true
per_page: 10
---

- if paginate
  %p
    Page #{page_number} of #{num_pages}
  - if prev_page
    %p= link_to 'Previous page', prev_page
- page_articles.each_with_index do |article, i|
  %article
    %h2
      = link_to summary_title(article), article
      %span= format_date(article.date, :short)
    ~ article.summary
- if paginate
  - if next_page
    %p= link_to 'Next page', next_page
